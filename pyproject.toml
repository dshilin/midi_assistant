[project]
name = "midi-assistant"
version = "0.1.0"
description = "AI консультант по строительным материалам"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "paramiko>=5.0.0",
]
